import styles from '../styles/footer.module.scss';

const Footer = () => {

    return (
        <footer className={ styles.footer }>
            Placeholder
        </footer>
    );
}

export { Footer as default }