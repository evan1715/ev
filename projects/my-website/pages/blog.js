import styles from '../styles/evan.module.scss';

const Blog = () => {

    return (
        <main className={ styles.main }>
            Evan's blog page
        </main>
    )
}

export { Blog as default }